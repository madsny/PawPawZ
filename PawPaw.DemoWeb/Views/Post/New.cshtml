@model PawPaw.Posts.Post

<h2>New post</h2>
@using (Html.BeginForm("New", "Post", FormMethod.Post))
{
    <h4>Post</h4>
    @Html.LabelFor(model => model.Body, "body")
    @Html.TextAreaFor(model => model.Body)
    <input type="submit" value="Create" />
}
